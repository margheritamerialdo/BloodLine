function expandPerm() {
    var element = document.querySelector('.esclusione-perm');
    element.classList.toggle("expanded");
}

function expandTemp() {
    var element = document.querySelector('.esclusione-temp');
    element.classList.toggle("expanded");
}